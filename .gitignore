# PlatformIO build files
.pio/

# VSCode workspace settings
.vscode/
.code-workspace
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Other compiled objects and binaries
*.o
*.a
*.so
*.lib
*.bin
*.elf
*.hex
*.uf2

# Retired per-board example sketches - kept locally for reference only,
# not synced to GitHub (superseded by the current BSP-driven fleet code).
legacy_testing/

# Local-only dev WiFi credentials for STA connect testing - never commit
# real AP credentials. See ConnectivityDefaults.h for the __has_include
# pattern that picks this up when present.
components/Fleet_Connectivity/ConnectivityLocalSecrets.h
# Reference clones of other projects, for reading only - never part of this
# repo. Cloned here so they can be read locally at zero network cost; see
# docs/ROADMAP.md Q10. Nested git repos, so tracking them would recreate the
# no-.gitmodules gitlink problem Phase 0 fixed.
reference/
